import type { NextPage } from 'next';
import Header from '../components/layout/Header';

const Home: NextPage = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Home;
